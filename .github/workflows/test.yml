name: Tests

on:
  workflow_call #Тест вызываеться другим yml файлом (ci_cd.yml)

jobs:
  Test-1:
    name: Test-1
    runs-on: self-hosted
    steps:
      - name: Run test
        run: |
          echo "First test run"
  Test-2:
    name: Test-2
    runs-on: self-hosted
    steps:
      - name: Run test
        run: |
          echo "Second test run"
